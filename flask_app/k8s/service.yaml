apiVersion: v1
kind: Service
metadata:
  name: cve-flask-app
  namespace: postgres-db
  labels:
    app: cve-flask-app
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
  selector:
    app: cve-flask-app